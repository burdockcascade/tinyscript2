function i_do_nothing() {
    assert false;
}

function func1(bool, ignore_this_param) {
    assert bool;
}

function ignore_me() { assert false; }

function execute(fn) {
    fn(true);
}

function loopy(b) {
    var b_count = 0;

    for (item in b) {
        assert item <= 9;
        assert item >= 1;
        b_count = b_count + 1;
    }
}

function main(argv) {

    loopy([1,2,4,5,7,8,9]);

    func1(true, false);

    var f = function(bool) {
        assert bool;
    };

    f(true);

    var x = f;
    x(true);

    execute(f);
    execute(x);

    execute(function(b3) {
        assert true;
    });

    var table = {
        "f1": function(b5) {
            assert b5;
        }
    };

    var func1 = table["f1"];
    func1(true);

}