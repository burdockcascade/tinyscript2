function i_do_nothing()
    assert false
end

function func1(bool)
    assert bool
end

function ignore_me() assert false end

function execute(fn)
    fn(true)
end

function loopy(b)
    var b_count = 0

    for item in b do
        assert item >= 1
        assert item <= 9
        b_count = b_count + 1
    end
end

function main(argv)

    loopy([1,2,4,5,7,8,9])

    func1(true)

    var f = function(bool)
        assert bool
    end

    f(true)

    var x = f
    x(true)

    execute(f)
    execute(x)

    execute(function(bool)
        assert true
    end)

    var class = {
        "f1": function(bool)
            assert bool
        end
    }

    var cf = class["f1"]
    cf(true)

end