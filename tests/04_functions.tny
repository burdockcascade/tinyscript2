function i_do_nothing() {
    assert false;
}

function func1(bool, ignore_this_param) {
    assert bool;
}

function i_return_what_you_give_me(b) {
    return b;
}

function ignore_me() { assert false; }

function execute(fn) {
    fn(true);
}

function loopy(b) {
    var b_count = 0;

    for (item in b) {
        assert item <= 9;
        assert item >= 1;
        b_count = b_count + 1;
    }

    return b_count;
}

function do_loopy(val) {
    return loopy(val);
}


function main() {

    var count = do_loopy([1,2,4,5,7,8,9]);
    assert count == 7;

    func1(true, false);

    var f = function(bool) {
        assert bool;
    };

    f(true);

    var x = f;
    x(true);

    execute(f);
    execute(x);

    execute(function(b3) {
        assert true;
    });

    assert i_return_what_you_give_me(true);

    var table = {
        "f1": function(b5) {
            assert b5;
        }
    };

    var func1 = table["f1"];
    func1(true);

    stacky(100);

}

function stacky(s) {
    if s == 0 {
        return false;
    }

    stacky(s - 1);
}